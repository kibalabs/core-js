{
  "name": "@kibalabs/core",
  "version": "0.5.0",
  "scripts": {
    "build": "build-module",
    "start": "build-module --start",
    "start-dev": "build-module --start --dev",
    "publish": "kiba-publish"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*"
  ],
  "dependencies": {
    "date-fns": "^2.14.0",
    "date-fns-tz": "^1.0.10",
    "deepmerge": "^4.2.2",
    "fast-deep-equal": "^3.1.3"
  },
  "devDependencies": {
    "@kibalabs/build": "0.4.0"
  }
}
